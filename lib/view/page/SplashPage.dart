import 'package:flutter/material.dart';
import 'package:technicaltestfan/view/widget/SpalashWidget.dart';

class SpalashPage extends StatelessWidget {
  const SpalashPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SplashWidget(),
    );
  }
}